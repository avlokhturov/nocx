{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "shell.environmentObserved.schema.json",
  "title": "shell.environmentObserved result",
  "description": "The renderer's report of what a minted attempt produced, crossing the control plane as a typed observation (2026-08-05 delivery-modes design §5.4 — the AD-1 amendment that admits it is named in the P7 commit). The renderer sends an accepted readiness passport (the tracker accepted it, so its id is the id nocx minted for the attempt in flight), or a null passport when the attempt ended with none. Only an attempt the backend minted can change state; the first observation per attempt decides it.",
  "type": "object",
  "additionalProperties": false,
  "required": ["processed", "factUpdated"],
  "properties": {
    "processed": {
      "description": "Whether the environmentId matched a live minted attempt. false means the report is stale or foreign (typically after a backend restart) — the renderer logs it and nothing is written.",
      "type": "boolean"
    },
    "factUpdated": {
      "description": "Whether the durable installed-fact store changed: an accepted passport recorded the installation, or a no-passport report invalidated a fact that had expected installed-script. false for a processed report that wrote nothing (e.g. a duplicate, or a bootstrap attempt ending without a passport).",
      "type": "boolean"
    }
  }
}
