{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/export.import.schema.json",
  "title": "ImportResult",
  "description": "Result of the export.import JSON-RPC method: what a configuration export restored. Settings are restored alongside the profiles and groups but are not counted here — their presence is not the interesting number, and a half-restored settings document is an error, not a count.",
  "type": "object",
  "additionalProperties": false,
  "required": ["profilesImported", "groupsImported"],
  "properties": {
    "profilesImported": {
      "description": "SSH connection profiles restored (created or replaced).",
      "type": "integer"
    },
    "groupsImported": {
      "description": "Profile groups restored (created or replaced).",
      "type": "integer"
    }
  }
}
