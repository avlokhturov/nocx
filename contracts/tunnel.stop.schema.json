{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/tunnel.stop.schema.json",
  "title": "TunnelStopResult",
  "description": "Result of the tunnel.stop JSON-RPC method: the record of the stopped forward. Same shape as tunnel.open's result — the renderer updates the row in place with the returned state.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "direction", "requestedBind", "actualBind", "destination", "scope", "state", "stopReason", "error"],
  "properties": {
    "id": {
      "description": "Backend-assigned tunnel id.",
      "type": "string"
    },
    "direction": {
      "description": "Forwarding strategy. Only local (-L) is implemented.",
      "type": "string",
      "enum": ["local", "remote", "dynamic"]
    },
    "requestedBind": {
      "description": "The bind the caller asked for at open. Port 0 means 'allocate'; the usable address is actualBind.",
      "type": "object",
      "additionalProperties": false,
      "required": ["host", "port"],
      "properties": {
        "host": {
          "type": "string"
        },
        "port": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "actualBind": {
      "description": "The address the listener held while running. The OS-assigned port when the request was 0.",
      "type": "object",
      "additionalProperties": false,
      "required": ["host", "port"],
      "properties": {
        "host": {
          "type": "string"
        },
        "port": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "destination": {
      "description": "Remote target of the forward, host:port.",
      "type": "string"
    },
    "scope": {
      "description": "Owner label the renderer attached at open.",
      "type": "string"
    },
    "state": {
      "description": "Lifecycle state; always stopped for a successful stop.",
      "type": "string",
      "enum": ["starting", "running", "stopped"]
    },
    "stopReason": {
      "description": "Why the forward stopped. A user stop is 'user'; a transport death is 'connection lost'; a bind failure is 'error'.",
      "type": ["string", "null"],
      "enum": ["user", "connection lost", "error", null]
    },
    "error": {
      "description": "The error behind the stop, when the stop was not clean.",
      "type": ["string", "null"]
    }
  }
}
