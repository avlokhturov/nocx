{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/connections.passwordRequest.schema.json",
  "title": "ConnectionsPasswordRequest",
  "description": "Params of the connections.passwordRequest server-to-client notification. The backend asks the renderer for a connection password, naming which connection and account it is asking about (nocx-s8jn — every password prompt must say which password it is asking for); the renderer turns this into the connection-password prompt and answers via connections.passwordResolved.",
  "type": "object",
  "additionalProperties": false,
  "required": ["requestId", "connection", "user", "host", "reason"],
  "properties": {
    "requestId": {
      "description": "Server-assigned opaque id that the connections.passwordResolved response echoes back.",
      "type": "string"
    },
    "connection": {
      "description": "The saved profile's display name the prompt is asking for. Empty for asks that do not name a profile (never sent today — direct-host opens do not raise prompts).",
      "type": "string"
    },
    "user": {
      "description": "The account on the host the password belongs to. The same host with a different user is a different password.",
      "type": "string"
    },
    "host": {
      "description": "The resolved host the password will authenticate against.",
      "type": "string"
    },
    "reason": {
      "description": "Why the password is being asked: no password is stored for the connection, or the stored one was rejected. A human-readable sentence, never a code.",
      "type": "string"
    }
  }
}
