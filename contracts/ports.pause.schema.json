{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/ports.pause.schema.json",
  "title": "PortsPauseResult",
  "description": "Result of the ports.pause JSON-RPC method: an empty acknowledgement. The method's only observable effect is the scheduler's paused flag, which the next ports.status carries.",
  "type": "object",
  "additionalProperties": false,
  "required": []
}
